#  install docker on ubuntu 20.04 server
sudo snap install docker

# pull and run an SQL image from docker hub 


#  create docker network 
docker network create --driver bridge pushtosite-network

#create the named volume push-to-site-db
docker volume create push-to-site-db

# link container to network on run | 3306 is exposed 
docker run --name mysql --network pushtosite-network -e MYSQL_ROOT_PASSWORD=YKouv23KTRnvP2pRYEzi -d -v push-to-site-db:/var/lib/mysql mysql:8


 
